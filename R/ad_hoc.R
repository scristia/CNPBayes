label_components <- function(model) {
    labels <- c("Homozygous Deletion",
                "Hemizygous Deletion",
                "Normal",
                "Copy Number Gain")

    return(labels)
}
